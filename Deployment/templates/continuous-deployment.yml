parameters:
  - name: ContinueEvenIfResourcesAreGettingDestroyed
    type: boolean
    default: false
  - name: AzureSubscription
    type: string
  
steps:
   # Step 1: Temporarily enable public access to KV and Storage Account
   - task: AzureCLI@2
     displayName: "Enable Public Access (Temporary)"
     inputs:
       azureSubscription: ${{parameters.AzureSubscription}}
       scriptType: 'pscore'
       scriptLocation: 'scriptPath'
       scriptPath: '$(Pipeline.Workspace)/terraformartifact/enable_public_access.ps1'
       arguments: '-keyVaultName $(DeploymentKeyVaultName) -storageAccountName $(DeploymentStorageAccountName) -resourceGroupName $(DeploymentResourceGroupName)'
     continueOnError: false

   # Step 2: Deploy Terraform (now KV and SA are accessible)
   - task: PowerShell@2
     displayName: "Terraform deployment"
     name: TerraformDeploy
     inputs:
       targetType: filePath
       filePath: '$(Pipeline.Workspace)/terraformartifact/terraform_conditional_run.ps1'
       arguments: '-deploymentResourceGroupName $(DeploymentResourceGroupName) -deploymentStorageAccountName $(DeploymentStorageAccountName) -workSpace $(Environment) -continueEvenIfResourcesAreGettingDestroyed $${{ parameters.ContinueEvenIfResourcesAreGettingDestroyed }} -terraformJsonOutputFile $(Pipeline.Workspace)/terraformartifact/terraform_output.json'
     env:
       ARM_CLIENT_ID: $(TERRAFORM-CLIENT-ID)
       ARM_CLIENT_SECRET: $(TERRAFORM-CLIENT-SECRET)
       ARM_TENANT_ID: $(TERRAFORM-TENANT-ID)
       ARM_SUBSCRIPTION_ID: $(TERRAFORM-SUBSCRIPTION-ID)
       ARM_ACCESS_KEY: $(TERRAFORM-ACCESS-KEY)
       TF_VAR_ARM_OBJECT_ID: $(TERRAFORM-CLIENT-OBJECT-ID)
       TF_VAR_spoke_rg: $(spokeRG)
       TF_VAR_spoke_vnet_name: $(spokeVnetName)
       TF_VAR_spoke_subnet_name: $(spokeSubnetName)
       TF_VAR_subscription_id: $(subscriptionId)
       TF_VAR_hub_subscription_id: $(hubSubscriptionId)
       TF_VAR_pe_vnet_name: $(peVnetName)
       TF_VAR_pe_subnet_name: $(peSubnetName)
       TF_VAR_pe_rg: $(peRG)
       TF_VAR_autotest_object_id: $(EntraID.AutoTest.ObjectIdentifier)
       TF_VAR_dns_zone_rg: $(dnsZoneRG)

   # Step 3: Disable public access (lock down to private endpoints only)
   - task: AzureCLI@2
     displayName: "Disable Public Access (Private Endpoint Only)"
     inputs:
       azureSubscription: ${{parameters.AzureSubscription}}
       scriptType: 'pscore'
       scriptLocation: 'scriptPath'
       scriptPath: '$(Pipeline.Workspace)/terraformartifact/disable_public_access.ps1'
       arguments: '-keyVaultName $(DeploymentKeyVaultName) -storageAccountName $(DeploymentStorageAccountName) -resourceGroupName $(DeploymentResourceGroupName)'
     condition: always() # Run even if Terraform fails, to ensure lockdown
